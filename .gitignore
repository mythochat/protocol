# macOS
.DS_Store

# Archives
*.zip
*.tar
*.tar.gz
*.tgz

# Node tooling
node_modules/
/pnpm-lock.yaml
/package-lock.json
/yarn.lock
# Note: scripts/ tooling is an isolated subpackage; its lockfile IS tracked
# for CI reproducibility (scripts/pnpm-lock.yaml). Only repo-root lockfiles
# (none expected; this repo is spec, not an npm package) are ignored.

# Local agent / audit state (never publish)
.agent/
.tmp-agent/
walkthrough.md
HANDOFF-*.md

# Editor
.vscode/
.idea/
*.swp
